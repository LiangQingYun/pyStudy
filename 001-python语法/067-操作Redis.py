"""
    字符串类型（string）：存储字符串、整数或浮点数。
    列表类型（list）：存储有序的字符串列表。
    集合类型（set）：存储无序的字符串集合，支持交集、并集、差集等集合操作。
    有序集合类型（sorted set）：类似于集合类型，但是每个元素带有一个权重（score），可以按照权重排序。
    哈希表类型（hash）：存储键值对，可以看作是一个字典或者散列表。
    位图类型（bitmap）：存储位的数据结构，支持位操作。
"""
