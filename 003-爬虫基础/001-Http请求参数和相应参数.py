"""
    HTTP Headers 是在 HTTP 请求和响应消息头中用于传输元数据的数据结构。常见的请求头包括：

    User-Agent：浏览器或其他客户端应用程序的标识字符串
    Accept：请求的 MIME 类型（如 text/html, application/json）
    Content-Type：请求体中的 MIME 类型（如 application/x-www-form-urlencoded, application/json）
    Authorization：认证信息
    Cookie：保存在客户端的 Cookies 信息
    常见的响应头包括：

    Content-Type：响应的 MIME 类型（如 text/html, application/json）
    Content-Length：响应内容的长度
    Server：服务器的标识字符串
    Set-Cookie：设置在客户端的 Cookies 信息
    Cache-Control：控制缓存的行为

    不清楚的就直接全部搞上去再修改对应变动的参数
"""

"""
    请求分为请求URL  请求方式 , 请求头, 请求体
"""


